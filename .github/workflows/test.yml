name: Test workflow
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  echo:
    name: Echo
    runs-on: ubuntu-22.04
    steps:
      - name: Test
        run: echo "Test run"
